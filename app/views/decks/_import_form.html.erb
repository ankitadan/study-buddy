<%= form_with url: import_deck_cards_path(@deck), multipart: true, class: "import-form" do |form| %>
  <label class="icon-button icon-button-labeled"
         title="CSV columns: question, answer">
    <%= icon(:upload) %> Import Cards (CSV)
    <%= form.file_field :file,
        accept: ".csv,text/csv",
        class: "visually-hidden",
        onchange: "this.form.requestSubmit()" %>
  </label>

  <noscript><%= form.submit "Import" %></noscript>
<% end %>